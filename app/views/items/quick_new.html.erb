<div class="max-w-md mx-auto bg-white shadow-md rounded-lg p-6 mt-8">
  <h1 class="text-xl font-bold text-center mb-4">✨ 最初のアイテムを登録しよう ✨</h1>
  <p class="text-center text-gray-600 mb-6 text-sm">
    画像かタイトルどちらかだけでもOK！あとで詳しく編集できます。
  </p>

  <%= form_with model: @item, url: quick_create_items_path, local: true do |f| %>
    <div class="mb-4">
      <%= f.label :image, "画像（任意）", class: "block text-gray-700 text-sm mb-1" %>
      <%= f.file_field :image, class: "w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :name, "タイトル（任意）", class: "block text-gray-700 text-sm mb-1" %>
      <%= f.text_field :name, class: "w-full border rounded p-2" %>
    </div>

    <div class="text-center mt-6">
      <%= f.submit "登録する", class: "bg-pink-500 hover:bg-pink-600 text-white font-bold py-2 px-6 rounded-md transition" %>
    </div>
  <% end %>
</div>
